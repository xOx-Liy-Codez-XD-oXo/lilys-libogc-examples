# Pixel lighting
The Wii's lighting is per-vertex, but reflection mapping can vary per-texel which can occur more often. In this example, we render a sphere to an offscreen buffer, then use it as a reflection map.
